# GOAI evidence assets

This directory is reserved for final submission evidence captured from a verified `product/xiaotong` build.

Before submission, place only verified assets here, such as the 180-second demo recording, a cover image, product screenshots, and an optional architecture export. The matching XIAOTONG license, additional terms, source link, author/contact and About access must travel with every distributed product artifact that shows those frames. Do not add API keys, access codes, personal files, unredacted traces or invented dashboards. Every quantitative claim should link back to `../07-evaluation-report.md` or another reproducible artifact in the repository.
