# Gold labels

Gold labels live beside each synthetic input in `cases/index.mjs` so reviewers can inspect an input and its expected tasks, deadline fields, deliverables, clarification decision, and conflict flag together. The fixed reference clock is `2026-08-06T10:00:00+08:00` in `Asia/Shanghai`.
