# Fixtures

The initial benchmark uses synthetic plain-text and OCR-transcript fixtures embedded in `cases/index.mjs`. OCR cases measure robustness after OCR text is available; they do not measure the Tesseract image-recognition stage. Real-image OCR accuracy remains separately unmeasured.
