{
  "name": "@chroni/desktop",
  "version": "0.1.3",
  "private": true,
  "description": "Local-first desktop deadline agent with document extraction, daily planning, and companion reminders.",
  "license": "MIT",
  "author": "Chroni contributors",
  "homepage": "https://github.com/miracle121388-a11y/chroni#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/miracle121388-a11y/chroni.git"
  },
  "bugs": {
    "url": "https://github.com/miracle121388-a11y/chroni/issues"
  },
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "pnpm run dev:desktop",
    "start": "pnpm run build && node scripts/run-electron.cjs .",
    "dev:renderer": "vite --host 127.0.0.1",
    "dev:electron": "wait-on http://127.0.0.1:5173 && pnpm run build:main && cross-env CHRONI_RENDERER_URL=http://127.0.0.1:5173 node scripts/run-electron.cjs .",
    "dev:desktop": "concurrently -k -s command-electron -n renderer,electron -c cyan,magenta \"pnpm run dev:renderer\" \"pnpm run dev:electron\"",
    "build:main": "tsc -p tsconfig.json",
    "build:renderer": "vite build",
    "build": "pnpm run build:main && pnpm run build:renderer",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.renderer.json",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "clean:package": "node scripts/clean-package-output.mjs",
    "package": "pnpm run clean:package && pnpm run build && electron-builder --config electron-builder.config.cjs --publish never",
    "package:win": "pnpm run clean:package && pnpm run build && electron-builder --config electron-builder.config.cjs --win --x64 --publish never",
    "package:mac": "pnpm run clean:package && pnpm run build && electron-builder --config electron-builder.config.cjs --mac --universal --publish never",
    "package:linux": "pnpm run clean:package && pnpm run build && electron-builder --config electron-builder.config.cjs --linux --x64 --publish never"
  },
  "dependencies": {
    "electron-updater": "6.8.9",
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "read-excel-file": "^9.2.0",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "5.2.8",
    "@fontsource-variable/noto-sans-sc": "5.2.10",
    "@types/node": "^25.6.2",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^42.0.0",
    "electron-builder": "^26.10.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "wait-on": "^9.0.10"
  }
}
